# Build artifacts
/helmsman
/dist/
*.exe

# Local runtime state / databases
*.db
*.db-wal
*.db-shm

# Editor / OS
.DS_Store
*.swp

# Local agent/tooling config (not shared)
/.claude/

# Local dev config (the real one lives at /etc/helmsman/config.yaml, never in-repo)
/config.local.yaml

# APT repo output from deploy/apt/publish.sh (pushed to the gh-pages branch, not main)
/public/

# Local working plan / scratch patch docs (never committed). Root-only glob so it
# never matches tracked docs under docs/.
/M20_PLAN.md
/helmsman-*-patch.md
